using Rodolfo

main do
  run do
    get "/" do |_|
      "Rodolfo says hello!"
    end

    get "/hello/:name" do |env|
      "Hello, ${env.param("name").try}!"
    end

    post "/echo" do |env|
      env.request.body.to(String).try
    end
  end.try
end
